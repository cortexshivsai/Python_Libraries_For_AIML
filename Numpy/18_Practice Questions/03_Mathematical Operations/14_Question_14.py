#Find the Cumulative sum.
import numpy as np
a = np.array([10, 20, 30, 40, 50])
b = np.array([1, 2, 3, 4, 5])
print("Cumulaive sum of array a: ",np.cumsum(a))
print("Cumulaive sum of array b: ",np.cumsum(b))